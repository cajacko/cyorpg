import { Action } from 'redux';

/**
 * Stories reducer
 */
const reducer = (state = {}, action: Action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;
